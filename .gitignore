# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# testing / coverage
/coverage

# secrets — NEVER commit. Real keys live only in .env.local
.env
.env.local
.env*.local
# Firebase Admin service-account JSON (if you download one instead of using env vars)
*serviceAccount*.json
*-firebase-adminsdk-*.json

# deploy / vercel
.vercel

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
